# Used automatically by `npm run build` (production mode).
# Dev (`npm run dev`) ignores this and uses the Vite proxy to localhost:8000.
# REST calls hit https://…/api/v1 and the WebSocket becomes wss://…/ws/…
VITE_API_URL=https://spinqueue-server-production.up.railway.app
